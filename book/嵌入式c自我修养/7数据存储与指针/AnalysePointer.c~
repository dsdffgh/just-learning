#include<stdlib.h>
int main() {
	  /********************指针数组*******************/
	  int num=10086;
	    int *pointer = malloc(4);
	      int *a[10];
	        a[1]=10;//means a pointer points to memory that is either unallocated or misaligned with data boundaries. 
			 *a = &num;
			   *pointer = 256;
			     a[1] = pointer;
			       pointer = 10;
			         //*pointer = 100; //
			           /********************数组指针*******************/
			             int ano[10];
			               int (*b)[10];
			                 b = ano;
			                   //b[0] = 9;
			                     *b[1] = 8;
			                       int ano_num[2][10];
			                         int (*c)[10] = ano_num;
			                           c[3] = 100;
			                             *c[8] = 8;
			                               return 0;
			                               }
